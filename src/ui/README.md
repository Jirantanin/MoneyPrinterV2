# Studio UI Components

- `podcast_ui.html`: main layout shell used by the backend route
- `podcast_component.html`: podcast tab markup and podcast-specific client logic
- `shorts_component.html`: shorts tab markup, shared tab switching, and shorts-specific client logic

These files are assembled server-side in `src/podcast_server.py`.

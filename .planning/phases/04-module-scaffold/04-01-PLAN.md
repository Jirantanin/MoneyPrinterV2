---
phase: 04-module-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/classes/Podcast.py
autonomous: true
requirements:
  - MOD-02

must_haves:
  truths:
    - "src/classes/Podcast.py exists as a standalone file importable from main.py"
    - "Podcast class exposes generate_script(), generate_assets(), render(), upload(), and run() methods"
    - "All step methods raise NotImplementedError with a message identifying the phase (e.g. 'Phase 5: not yet implemented')"
    - "run() calls the four step methods in order without adding logic of its own"
    - "The class imports llm_provider.generate_text, Tts from classes.Tts, and get_podcast_narrator/get_podcast_style_prompt from config — no YouTube.py import"
  artifacts:
    - path: "src/classes/Podcast.py"
      provides: "Podcast pipeline class — stub implementation for Phase 4"
      exports: ["Podcast"]
  key_links:
    - from: "src/classes/Podcast.py"
      to: "src/llm_provider.py"
      via: "from llm_provider import generate_text"
      pattern: "from llm_provider import generate_text"
    - from: "src/classes/Podcast.py"
      to: "src/classes/Tts.py"
      via: "from classes.Tts import TTS"
      pattern: "from classes.Tts import TTS"
---

<objective>
Create src/classes/Podcast.py as a standalone class with the four-step interface decided in D-01/D-02 and NotImplementedError stubs per D-03.

Purpose: Defines the class contract that phases 5-8 fill in one method at a time. Without this file, Plan 02 cannot import Podcast in main.py.
Output: src/classes/Podcast.py — importable, stub class with correct interface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-module-scaffold/04-CONTEXT.md

<interfaces>
<!-- Existing patterns the executor must replicate exactly. -->

From src/classes/Tts.py import pattern (used in YouTube.py):
```python
from classes.Tts import TTS
```

From src/llm_provider.py import pattern (used in main.py):
```python
from llm_provider import generate_text
```

From src/config.py getter pattern — every getter re-reads config.json on every call:
```python
def get_tts_voice() -> str:
    with open(os.path.join(ROOT_DIR, "config.json"), "r") as file:
        return json.load(file).get("tts_voice", "Jasper")

def get_tts_rate() -> str:
    with open(os.path.join(ROOT_DIR, "config.json"), "r") as file:
        return json.load(file).get("tts_rate", "+20%")
```

Config getters are imported via `from config import *` in main.py.
The new podcast getters (get_podcast_narrator, get_podcast_style_prompt) will be added in Plan 02.
Import them here in Podcast.py with a forward-compatible import:
```python
from config import get_podcast_narrator, get_podcast_style_prompt
```
These will exist by the time Plan 02 completes — stub the import now so the class is wired correctly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Podcast.py stub class</name>
  <files>src/classes/Podcast.py</files>
  <read_first>
    - src/classes/YouTube.py (reference for class structure, docstring style, import layout — do NOT modify)
    - src/classes/Tts.py (import pattern to replicate)
    - src/llm_provider.py (import pattern to replicate)
    - src/config.py (getter pattern; note that get_podcast_narrator and get_podcast_style_prompt do not yet exist — import them anyway for forward compatibility)
  </read_first>
  <action>
Create src/classes/Podcast.py with the following exact structure:

```python
"""
Podcast.py — Video Podcast pipeline class.

Standalone class (does not extend YouTube.py). Each public step method
corresponds to one downstream phase:
  generate_script()  → Phase 5
  generate_assets()  → Phase 6
  render()           → Phase 7
  upload()           → Phase 8

run() calls all four steps in order.
"""

import os
from config import get_podcast_narrator, get_podcast_style_prompt
from llm_provider import generate_text
from classes.Tts import TTS


class Podcast:
    """Automates the full Video Podcast pipeline: script → assets → render → upload."""

    def __init__(self) -> None:
        self.narrator = get_podcast_narrator()
        self.style_prompt = get_podcast_style_prompt()

    def generate_script(self) -> None:
        """Generate a 14-scene podcast script (intro + 3 acts + outro).

        Raises:
            NotImplementedError: Phase 5 not yet implemented.
        """
        raise NotImplementedError("Phase 5: not yet implemented")

    def generate_assets(self) -> None:
        """Generate per-scene images (Gemini) and TTS audio (edge-tts).

        Raises:
            NotImplementedError: Phase 6 not yet implemented.
        """
        raise NotImplementedError("Phase 6: not yet implemented")

    def render(self) -> None:
        """Render 14 scene clips and concatenate via FFmpeg.

        Raises:
            NotImplementedError: Phase 7 not yet implemented.
        """
        raise NotImplementedError("Phase 7: not yet implemented")

    def upload(self) -> None:
        """Upload final MP4 and thumbnail to YouTube.

        Raises:
            NotImplementedError: Phase 8 not yet implemented.
        """
        raise NotImplementedError("Phase 8: not yet implemented")

    def run(self) -> None:
        """Run the full podcast pipeline: script → assets → render → upload."""
        self.generate_script()
        self.generate_assets()
        self.render()
        self.upload()
```

Important constraints:
- Do NOT import from YouTube.py (per MOD-02 and D-01/D-02 decisions)
- Stub messages must say "Phase N: not yet implemented" exactly so downstream phases can grep for remaining stubs
- The `run()` method must contain no logic beyond the four method calls — phases 5-8 fill in the step methods
- Import `get_podcast_narrator` and `get_podcast_style_prompt` even though they don't exist in config.py yet — Plan 02 adds them
  </action>
  <verify>
    <automated>cd /c/Users/66984/workspace-coding/MoneyPrinterV2 && py -c "import ast; ast.parse(open('src/classes/Podcast.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/classes/Podcast.py exists
    - File contains `class Podcast:` declaration
    - File contains `def generate_script(self)` method
    - File contains `def generate_assets(self)` method
    - File contains `def render(self)` method
    - File contains `def upload(self)` method
    - File contains `def run(self)` method
    - File contains `raise NotImplementedError("Phase 5: not yet implemented")`
    - File contains `raise NotImplementedError("Phase 6: not yet implemented")`
    - File contains `raise NotImplementedError("Phase 7: not yet implemented")`
    - File contains `raise NotImplementedError("Phase 8: not yet implemented")`
    - File contains `from llm_provider import generate_text`
    - File contains `from classes.Tts import TTS`
    - File contains `from config import get_podcast_narrator, get_podcast_style_prompt`
    - File does NOT contain `from classes.YouTube import` or `import YouTube`
    - Python syntax check passes (`py -c "import ast; ast.parse(...)"` exits 0)
  </acceptance_criteria>
  <done>Podcast.py exists with correct class interface, all four step methods raise NotImplementedError with phase-labeled messages, run() calls them in order, no YouTube.py import.</done>
</task>

</tasks>

<verification>
- grep -n "class Podcast:" src/classes/Podcast.py
- grep -n "def generate_script\|def generate_assets\|def render\|def upload\|def run" src/classes/Podcast.py
- grep -n "NotImplementedError" src/classes/Podcast.py  (should show 4 lines)
- grep -n "from llm_provider import\|from classes.Tts import\|from config import" src/classes/Podcast.py
- py -c "import ast; ast.parse(open('src/classes/Podcast.py').read()); print('OK')"
</verification>

<success_criteria>
src/classes/Podcast.py exists, is syntactically valid Python, exports class Podcast with five methods (generate_script, generate_assets, render, upload, run), all four step methods raise NotImplementedError with phase-labeled messages, and no YouTube.py import is present.
</success_criteria>

<output>
After completion, create .planning/phases/04-module-scaffold/04-01-SUMMARY.md
</output>
